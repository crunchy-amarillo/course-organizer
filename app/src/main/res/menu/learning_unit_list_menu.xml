<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/menuGroup1">
        <android:item
            android:id="@+id/menuUser"
            iconTint="@color/black"
            android:icon="@android:drawable/ic_dialog_info"
            android:title="@string/loggedin_user"
            app:showAsAction="never" />
    </group>

    <group android:id="@+id/menuGroup2">
        <android:item
            android:id="@+id/btnCourseList"
            iconTint="@color/black"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/title_activity_course_list"
            app:showAsAction="never" />

        <android:item
            android:id="@+id/btnExport"
            iconTint="@color/black"
            android:icon="@android:drawable/ic_menu_set_as"
            android:title="@string/export"
            app:showAsAction="never" />

        <android:item
            android:id="@+id/btnExportAndMail"
            iconTint="@color/black"
            android:icon="@android:drawable/ic_dialog_email"
            android:title="@string/export_pdf"
            app:showAsAction="never" />

        <android:item
            android:id="@+id/btnLogout"
            iconTint="@color/white"
            android:icon="@android:drawable/ic_lock_power_off"
            android:title="@string/logout"
            app:showAsAction="never" />
    </group>
</menu>